apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kokkak.fullname" . }}-secret
  labels:
    {{- include "kokkak.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secret }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
