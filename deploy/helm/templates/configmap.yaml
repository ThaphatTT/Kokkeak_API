apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kokkak.fullname" . }}-config
  labels:
    {{- include "kokkak.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
